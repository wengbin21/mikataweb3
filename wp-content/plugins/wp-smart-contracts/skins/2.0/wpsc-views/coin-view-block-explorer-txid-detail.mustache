		<div class="ui divider"></div>
		<div class="wps-tag">
			{{#isError}}
				<p>
					<i class="exclamation triangle icon"></i>
					{{{errorMessage}}}: {{{status}}}
				</p>
			{{/isError}}
			{{^isError}}
				<i class="{{icon}} icon"></i>
				{{{status}}}
			{{/isError}}
		</div>
